<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark/Night Theme -->
    <style name="Theme.AppDocTruyen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color (both naming styles for compatibility) -->
        <item name="colorPrimary">@color/primary_color_dark</item>
        <item name="colorPrimaryVariant">@color/primary_color_darker</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/accent_color_dark</item>
        <item name="colorSecondaryVariant">@color/accent_color_darker</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Background colors -->
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="android:textColorPrimary">@color/dark_text</item>
    </style>
</resources>